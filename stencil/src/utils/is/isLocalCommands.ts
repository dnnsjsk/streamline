// import { stateLocal } from '../store/local';
// import { state } from '../store/internal';

export function isLocalCommands() {
  /*
  let index = 0;

  Object.values(state.commands.local).forEach((item) => {
    if (item.condition !== false) {
      index++;
    }
  });

  return (
    state.menu[stateLocal.active].commands?.length >= 1 && index >= 1
  );
   */
}
